require "spec_helper"

describe Sogou::Tuiguang::Site do
  it "has a version number" do
    expect(Sogou::Tuiguang::Site::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
